{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permisos de la ventana principal. Deliberadamente sin `store:default` ni permisos de filesystem: esos permisos exponían settings_store.json (API keys, token MCP y external_script_path) o las grabaciones a una webview comprometida. La persistencia pasa por comandos validados y el audio del historial por un protocolo privado con descifrado por rangos.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "updater:default",
    "process:default",
    "dialog:default",
    "global-shortcut:allow-is-registered",
    "global-shortcut:allow-register",
    "global-shortcut:allow-unregister",
    "global-shortcut:allow-unregister-all",
    "macos-permissions:default",
    "dialog:allow-save"
  ]
}
